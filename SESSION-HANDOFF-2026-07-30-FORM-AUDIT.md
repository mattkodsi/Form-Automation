# Form-level audit — handoff, 2026-07-30 (morning session)

**Branch:** `worktree-rcs-corpus`, HEAD `ed0be74`, tree clean, everything pushed.
**Never push or merge to `main`** — a push to main is a production deploy.
**1,832 checks across eleven suites**, all green. Delivered and `cmp`-verified. RA anchors built.

---

## THE ONE-LINE STATE

Five defects fixed and pushed (M60–M65), all **branch-only** — Matt runs `main`, so none of it
has reached the app he is testing. Two questions are open and both need **Matt's answer, not more
of my guessing**: which file he uploaded for the Part G principal, and whether a freshly generated
schedule calculates in Acrobat.

---

## WHAT IS WAITING ON MATT

### 1. Two test files in `~/Downloads`, written 2026-07-30 11:05

| file | md5 | what it is |
|---|---|---|
| `RCS-TEST-1-our-template-untouched.pdf` | `769f71174779fc5fa64ba4e186d216e7` | byte-identical to HUD's own 92458.pdf |
| `RCS-TEST-2-generated-by-us.pdf` | `93344db3b0e2a6723070e85ae43af5b0` | generated by current code, Colonial Village data, both principals |

**Open in Acrobat. Do NOT open in Preview first.** Preview re-saves on close and strips every
calculation script — measured, see M66 below. Verify with `md5 -q <file>` before testing.

- If TEST-2 calculates → the defect is narrower than thought; chase it with his session's record.
- If TEST-1 calculates and TEST-2 does not → **pdf-lib's save is the culprit**, and that is the
  comparison that could not be made until Matt supplied HUD's original.

### 2. Which file did he upload for Colonial Village?

The 2023 **executed** copy has **no AcroForm fields** (flattened to outlines). The 2026 **draft**
has 232. Part G is only readable from fields. Measured today, feeding it the draft, **both
principals survive every stage** — so if he uploaded the executed copy, the second row's absence
comes from the text tier, not from any code inspected today. This decides where to look next.

### 3. Three decisions still deferred (do NOT take them)

1. Whether `uaBox`/`safmrBox` read the live matcher, keep reading the stored cell, or read the cell
   and say in the cell when the two disagree.
2. Whether `drive.js` starts recording the form record.
3. Whether the sweep exits non-zero on an order disagreement.

*(What a document PRINTS is no longer blanket-deferred — Matt has now specified Part A cols 3 & 5,
Part D col 3, the aka name in the Project Name, and that every calculated cell reads its zero.)*

### 4. Two branches awaiting review

- **`tab-order`** — pushed, NOT merged. Tab order and dropdown focus. See below.
- **`worktree-rcs-corpus`** — everything in M60–M65. Merging it is what puts the aka name, the
  Part A zeros, Part D's proposed rent, the dollar signs and the calculated-cell zeros in front of
  him.

---

## WHAT WAS FIXED, IN ORDER

### M60 `550a38f` — a page reload undid M59

`_rsFill`/`_rcsFill` were module variables holding the app's only record that a document had been
*applied* rather than *read*, while `rsRecall`/`rcsRecall` restored the readings beside them. A
reload kept the reading and dropped the record, so the roster re-read gated on it could not fire —
**Matt's studio/one-bedroom defect returned on the second sitting.** Proved in real chromium on a
real `Page.navigate`: `got ["Studio:1000","Studio:1500","1BR:","1BR:"]`. The same variables leaked
sideways: a study applied on property A applied itself unasked to property B. The record now
travels with its document, per cycle. `test_browser.js` 315 → 327, `smoke_combined.js` 165 → 173.

### M61 `5b9a4f1` — the durable record outlived the fill (my own defect, from M60)

A fill applied and never saved does not survive a reload, but the record did, so the tile read
*"Filled 10 values — 3 still to save."* over a form holding one empty row. The `3` counted nothing.
A record is now checked against the form before it is believed: at least one key it names must
still carry the document's value. Three cases measured (unsaved retires, saved survives, saved
schedule survives — the third is the control that stops the rule over-reaching).

### M62 `b6b391e` — provenance painted twice, two cells painted differently

All 60 boxes of a four-row package enumerated: **2 moved by a whole colour** on repaint.
`units.0.ua_source` overridden-orange → new-grey; `units.0.safmr_source` teal → grey. `paintCell`
handed a `*_source` key to `srcCellState`, which wants a `*_custom` key, got null, and judged the
cell by its own history. Colour of a source-backed cell is a *family* question; now one function,
both painters. 0 of 60 move now, 0 of 35 after a reload. **The new test passed on the broken code
first time** — a `try/catch` swallowed the missing `__paintCell` door so no repaint fired. Step 4
caught it.

### M63 `b6b391e` — three of Matt's four Colonial Village findings

- **Part A cols 3 & 5 of a non-revenue row state `0`.** Settles the argument the old comment was
  stuck in across three properties: the question was never *which rent*, it is that the unit earns
  no contract rent and carries no allowance.
- **Part D col 3 is the PROPOSED rent**, not last term's. Leasing office printed 1,147; the filing
  says 1,850. Matches bedroom+bath first, bedroom alone second, stored rent last.
- **aka name in the Project Name** — already fixed in `03a5452`, absent from `main`.

### M64 `46ff332` — no dollar sign, and the sweep could not have seen it

`gen.js`'s rent-schedule `money` is dollar-less at line 258 here **and line 234 on `main`** — not a
branch gap. Missed because `extract.js:96` (`s.replace(/[$,]/g,'')`) and `compare.js:68`
(`replace(/^\$\s*/,'')`) strip currency from **both sides** before comparing. The comparator
compares figures and is blind to presentation, and no summary ever said so.

Eye-read from the executed 2023 copy: **per-row columns are bare, only the four potentials carry
the sign** (`$76,918`, `$923,016`, `$0`, `$0`). Hence `dmoney` on four fields, never a blanket `$`.
The old argument that market rent potential must print blank is **withdrawn** — filed copies print
`$0`.

### M65 `ed0be74` — a calculated cell reads its zero

From Matt's screenshots of the live form: **blue = input, white = calculable, and every white cell
reads its zero.** Measured against the blank template, which agrees exactly — 41 fields carry
`AFSimple_Calculate`; per row they are Col. 4 (`Col.2×Col.3`), Col. 6 (`Col.3+Col.5`), Col. 8
(`Col.7×Col.2`), all shipped as `"0"`. We cleared all eight columns of all eleven rows and blanked
`195`/`1156`. Now the five input columns blank and the three calculated ones read `0`.

The dollar sign splits the same way: on `174`/`195`/`1156` the `$` is **printed on the form beside
the box** so the field reads `0`; on the four potentials it is **inside the box** so the field
reads `$0`.

**Two flags withdrawn**, both mine: Part D Col. 2 prints `2 BR` on the live form (the bare `2` on
the 2023 copy was that PM's typing), and my own check *"no extension, because zero times anything
is not a claim"* was wrong — Col. 4 calculates, so it reads `0`.

---

## M66 — NOT YET WRITTEN TO THE REGISTER: Preview destroys the form

Measured, four files:

| file | version | `/CO` | calculate | format |
|---|---|---:|---:|---:|
| HUD official `92458.pdf` | 1.6 | 41 | 41 | 103 |
| our template, freshly decoded | 1.6 | 41 | 41 | 103 |
| **the copy handed to Matt, after Preview** | **1.3** | **0** | **0** | **0** |
| a schedule we generate | 1.7 | 41 | 41 | 103 |

**Our template is byte-identical to HUD's download** (`769f7117…` both). **Preview stripped all 41
calculation scripts** from the file handed to Matt and rewrote it as PDF-1.3, so his "your form
doesn't calculate anywhere" test was run against Preview's wreckage. My error: I handed him a file
through a path Preview could silently rewrite and did not checksum it before and after.

**This does NOT close the question.** Structure is not behaviour — that overreach was already made
once today. Whether a generated schedule actually calculates in Acrobat is what TEST-2 answers.

**Write this up as M66 once Matt reports back**, with his result, not before.

---

## THE PART G PRINCIPAL — measured, and not where anyone guessed

Colonial Village 2026 draft, raw fields:

```
206: "Colonial Village Preservation GP, LLC" / "General Partner"
208: "David Pearson, Vice President of General Partner" / ""      ← title empty, name intact
SIG: "Matthew Finkle, VP of GP" / ""
```

Every stage measured, none of them drops it:

- **reader** returns both principals (`__parseRsPdf` in a real browser)
- **`rsSigInto`'s filter** (`app.js:1077`) keeps it — the signatory is Matthew Finkle, not David
  Pearson, so the "signatory is not also a principal" rule does not fire
- **the fill** writes `principals.1.name` (the empty title is declined by `setk`, correctly)
- **the form** renders 2 principal rows
- **`gen.js:471`** writes field 208 even with an empty title — tested across three record shapes
- **pruning** keeps index 1, because `PRINCIPALS` is derived from the form

So on the draft it works end to end. **The 2023 executed copy has no form fields at all**, which is
the untested path and the likely one. Needs Matt's answer.

---

## THE OTHER AGENTS

### Property-name provenance — DONE, documented, nothing changed

`docs/superpowers/plans/2026-07-30-property-name-provenance.md`. Measured all 34; 24 readable, 10
are vector outlines.

- **7 of 24 print a CONTRACT NUMBER where the name goes** (`A / NY36A005001`, `A (IL060052016)`),
  and on **2 the shipped splitter files that contract number as `tenant.property_alias`** — which
  **prints on the tenant notice's letterhead**. Bigger than the alias problem Matt reported.
- 4 more are silently renamed today (RS name ≠ tracker name).
- Its rule scored **21 correct / 8 correctly refused / 0 wrong** over 29 cases; the shipped
  slash-only rule scored **11 correct / 18 wrong**.
- Two unfixed bugs found: `app.js:5172` writes a non-existent key `property.alias` (should be
  `tenant.property_alias`), and 75544 reads the blank template's caption `"ProjectName"` as its
  project name.
- Decisions laid out, **not taken**: tracker vs executed RS vs PM edit as definitive for
  `property.name`; whether PMs may rename (safe — 0 collisions across 249 — but churns 6 of 19 menu
  labels); what a refusal looks like (the rule refuses on 28% and there is nowhere for a refusal
  to go today).

### Tab order — DONE, branch `tab-order`, pushed, NOT merged

- Out-of-position stops in the five two-column sections **36 → 0**; wrong adjacent transitions
  **18 → 0**; unreachable 0 before and after; Shift-Tab already the exact reverse and still is.
- `renderFieldSection` emits column-by-column, `grid-auto-flow:column` places them. No handler
  intercepts Tab, so reverse order cannot drift. **25 cells measured at three widths, 0 moved a
  pixel.**
- **Roster grids deliberately stay row-major** (unit types, non-revenue, principals) — the row *is*
  the record. **Matt may want to overrule this.**
- Dropdown focus: **83 of 83 stops now carry an author-declared ring**, up from 6. Geometry
  measured, not asserted: **ring rect vs trigger rect max delta 0.00px** across all 83, contained
  in the cell, 10:1+ contrast on all five provenance backgrounds.
- 13 new checks, `MIN_CHECKS` 333 → 346, nine fail on pristine HEAD.

### The crypto check was never flaky — MERGED AND CLOSED (M66)

`crypto-nopad-fix` (`6b52af9`) is **merged into `worktree-rcs-corpus`**. The check that "randomly
aborted `deliver.sh`" was reporting a real defect the whole time: `aesDecryptCBC` stripped PKCS#7
padding on the `ivIncluded===false` path too — the revision 6 `UE`/`OE` strings, which are zero-IV,
**unpadded**, and whose plaintext *is* the 32-byte file key. One random key in sixteen ends on a
byte in `1..16`, gets read as a padding length, and comes back short; nothing raises and the
document decrypts to plausible garbage. Measured: **6.26% over 20,000 trials** against the 6.25%
predicted.

**Withdraw two things this handoff said.** "Fails under load and passes unchanged" and its
correction "a genuine flaky test on main, independent of load" were both wrong in the same way — a
failing test treated as a property of the harness, with nobody reading `crypto.js`. **The
`retry a red delivery ONCE` line in the loop prompt was a standing instruction to ignore this
defect and must not be carried forward.**

Seventeen deterministic checks now pin every ending `0x01..0x10` plus an all-valid-PKCS#7 tail; all
seventeen fail on `ed0be74`'s `crypto.js` (`X CRYPTO SUITE FAILED (17 of 98)`) and 60 consecutive
runs of the fixed suite are clean. `test_crypto.js` 81 → **98**. Bundle rebuilt and cmp-verified
byte-for-byte after the merge.

---

## THE STANDING METHOD LESSON, now three times over

The audit's instrument kept measuring the adjacent thing and reporting it as coverage:

1. **The comparator normalises presentation away**, so 34 properties of three-way comparison could
   not see a missing `$`. Never disclosed as a limitation.
2. **A new test passed on the broken code** because a `try/catch` swallowed a missing test door.
3. **"The calculation machinery survives"** was a claim about *structure* reported as a claim about
   *behaviour*. Matt's empirical test beat it.

**The rule that follows:** before believing a green check, ask what would have to be true for it to
be green *and* the thing still broken. And never hand Matt an artifact without a checksum on both
ends.

---

## THE QUEUE, in order

1. **Matt's TEST-1/TEST-2 result** → then M66, and the pdf-lib question if TEST-2 fails.
2. **Which file he uploaded** → then the Part G text-tier path.
3. **Teach the comparator to see presentation** — `extract.js:96` and `compare.js:68`. This is the
   real repair; M64 closed four cells, not the class. Hard part: most filed copies are vector
   outlines, so the raw string is often unavailable — the honest version may only cover the 9
   text-readable copies plus template-side assertions.
4. **Page 2 of Matt's screenshot** — never read. Part G is on it, and `174`/Part C were never
   audited against the live form.
5. **Phase plan** `docs/superpowers/plans/2026-07-30-form-inventory-audit.md`, phases 0–7. Phase 3d
   is done (M62). 3a/3b partly done (M60/M61).
6. Still-open register items: M51, M38, M42–M46, M48, M49, M53, M58.
7. **Peterson Plaza and Oaks on North Plaza** — fill orders still disagree by 17 and 16 rows, for a
   *different* mechanism than M59: the schedule says `2BR` with no bathroom while the study prices
   `2BR/1BA` and `2BR/1.5BA` separately, so the two orders build **different rosters**. Worth
   $27,850/month on Peterson's HUD form. Needs the two source documents read by eye.

---

# ADDENDUM — the calculation defect is SOLVED, and it is ours

Matt changed row 1's unit count from 33 to 3 in a generated schedule and photographed the result.
The arithmetic names the bug outright:

| cell | should be | our form computed |
|---|---|---|
| Col. 4 = Col.2 × Col.3 = 3 × 1,850 | 5,550 | **6** |
| Col. 6 = Col.3 + Col.5 = 1,850 + 160 | 2,010 | **162** |
| Monthly Contract Rent Potential | $5,550 | **$85** |

`3 × 1.85 = 5.55 → 6`. `1.85 + 160 = 161.85 → 162`. **Acrobat is reading our `1,850` as 1.85.**

## The mechanism

`AFNumber_Format(0, 0, 0, 0, "", false)` on the per-row cells and
`AFNumber_Format(0, 0, 0, 0, "$", true)` on fields 95–98. sepStyle **0** — the US convention.

A correctly filled form keeps the **raw number in `/V`** (`1850`) and lets the format action
*display* `1,850`. **We write the formatted string into `/V` itself.** `AFMakeNumber("1,850")`
returns 1.85, and every calculation downstream is wrong by a factor of a thousand.

Matt's second screenshot is the control: HUD's untouched template, his own typed input of 3 units
at 1850, computes 5,550 and $66,600 correctly — because typing leaves `/V` raw.

## THIS MAKES M64 WRONG, AND IT MUST BE REPLACED

M64 added `$` by writing `"$76,918"` into `/V`. That is an even less parseable string. The correct
fix is the one considered and wrongly rejected that morning: **write the raw number into `/V` and
let HUD's own format action render the separators AND the dollar sign.** The reason it was rejected
— that pdf-lib's baked appearance would then show bare digits in Preview/Chrome — is real but
secondary, and is solved by either setting `NeedAppearances`, or by keeping `/V` raw while baking a
formatted appearance stream (which is exactly what Acrobat itself produces).

**Do this before anything else in the queue.** It is the difference between a form a PM can work in
and a form that silently computes rents a thousandfold wrong on a federal filing.

Note the interaction with M65: the calculated cells now correctly read `0`, which is right, but the
*values we write into input cells* are what poison the arithmetic. Both must be raw.

---

# ADDENDUM — the executed copies are flattened, and the draft beside them is not

Matt uploaded the **2025** Colonial Village RS. Measured, that folder:

| file | fields | Part G |
|---|---:|---:|
| `White Oak Townhomes - Executed RS - FY2025.pdf` | **0** | 0 |
| `Colonial Village - Draft RS 2025 - (SIGNED).pdf` | 1 | 0 |
| **`Colonial Village - Draft RS 2025 - Unsigned.pdf`** | **232** | **2 principals, both intact** |

So the missing second principal is not a reader bug in the code paths traced earlier — **it is that
the executed copy carries no form fields at all**, and Part G is only recoverable from fields. The
unsigned draft in the same folder has everything.

**Matt's challenge — "your attempt to scan the text and not RCS is a bad move, been causing a lot
of issues, no?" — is correct**, and sharper than it sounds. Prior measurement: only ~9 of 34
executed schedules are text-readable at all; the rest are vector outlines needing OCR. The whole
tier-2/tier-3 apparatus exists to read a photograph of a document whose field-complete original is
often sitting in the same folder.

**Open design question for Matt, worth raising explicitly:** should the app ask for (or prefer) the
**unsigned draft** rather than the executed copy, falling back to scanning only when no draft
exists? That would make tier 1 the normal path instead of the exception. It needs a census — how
many of the 34 have a field-complete draft beside the executed copy — before it becomes a
recommendation. **That census is cheap and should be the next measurement.**
